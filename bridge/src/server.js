import crypto from "node:crypto";import http from "node:http";import { browserHealth,runChatJob } from "./chatgpt.js";import { getJob,patchJob,pruneJobs,putJob } from "./store.js";const port=Number(process.env.PORT||18791);const secret=process.env.BRIDGE_SHARED_SECRET||"";const queue=[];let active=false;
function json(res,status,body){res.writeHead(status,{"Content-Type":"application/json","Cache-Control":"no-store"});res.end(JSON.stringify(body))}function authorized(req){const token=String(req.headers.authorization||"").replace(/^Bearer\s+/i,"");if(!secret||token.length!==secret.length)return false;return crypto.timingSafeEqual(Buffer.from(token),Buffer.from(secret))}async function body(req){const chunks=[];for await(const chunk of req)chunks.push(chunk);const raw=Buffer.concat(chunks).toString("utf8");if(raw.length>30000)throw new Error("Request too large");return JSON.parse(raw||"{}")}
async function drain(){if(active)return;active=true;while(queue.length){const id=queue.shift();const job=getJob(id);if(!job||job.status!=="queued")continue;patchJob(id,{status:"generating",startedAt:Date.now()});try{const result=await runChatJob(job);patchJob(id,{status:"completed",output:result.output,conversationUrl:result.conversationUrl,completedAt:Date.now()})}catch(error){patchJob(id,{status:"failed",error:error instanceof Error?error.message:"Unknown bridge error"})}}active=false}
const server=http.createServer(async(req,res)=>{if(!authorized(req))return json(res,401,{error:"Unauthorized"});const url=new URL(req.url,`http://${req.headers.host||"localhost"}`);try{if(req.method==="GET"&&url.pathname==="/health")return json(res,200,{ok:true,queue:queue.length,active,...(await browserHealth())});if(req.method==="POST"&&url.pathname==="/jobs"){const input=await body(req);const required=["operationId","member","threadId","prompt"];if(required.some((key)=>!input[key]))return json(res,400,{error:"Missing required fields"});const duplicate=getJob(input.operationId);if(duplicate)return json(res,200,{jobId:duplicate.id,status:duplicate.status});const id=input.operationId;putJob({id,operationId:input.operationId,member:String(input.member),threadId:String(input.threadId),prompt:String(input.prompt),model:String(input.model||"auto"),status:"queued",createdAt:Date.now(),updatedAt:Date.now()});queue.push(id);void drain();return json(res,202,{jobId:id,status:"queued"})}const match=url.pathname.match(/^\/jobs\/([a-f0-9-]{36})$/i);if(req.method==="GET"&&match){const job=getJob(match[1]);if(!job)return json(res,404,{error:"Job not found"});if(url.searchParams.get("member")!==job.member)return json(res,403,{error:"Forbidden"});const{prompt,...safe}=job;return json(res,200,safe)}return json(res,404,{error:"Not found"})}catch(error){return json(res,500,{error:error instanceof Error?error.message:"Unknown error"})}});
pruneJobs(Number(process.env.JOB_RETENTION_HOURS||24));server.listen(port,"127.0.0.1",()=>console.log(`Family GPT Bridge listening on http://127.0.0.1:${port}`));
